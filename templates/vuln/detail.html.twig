{% extends 'base.html.twig' %}
{% block title %}SecureScan — {{ vuln.title|slice(0,60) }}{% endblock %}

{# Extraction des données selon l'outil #}
{% set raw      = vuln.rawData ?? {} %}
{% set cve      = raw.cve ?? null %}
{% set fixCmd   = raw.fix ?? null %}
{% set refUrl   = raw.url ?? null %}
{% set pkg      = raw.package ?? vuln.filePath %}
{% set version  = raw.version ?? (vuln.lineNumber ? 'Ligne ' ~ vuln.lineNumber : null) %}

{% block stylesheets %}
<style>
.detail-wrap { padding: 32px 40px; max-width: 1200px; margin: 0 auto; }
.breadcrumb {
  display: flex; align-items: center; gap: 8px;
  font-size: 0.8rem; color: var(--text3); margin-bottom: 28px;
}
.bc-link { color: var(--primary); text-decoration: none; font-weight: 500; }
.bc-link:hover { text-decoration: underline; }
.bc-sep { color: var(--border2); }

.detail-grid { display: grid; grid-template-columns: 1fr 300px; gap: 16px; }
.detail-left  { display: flex; flex-direction: column; gap: 14px; }

.d-card {
  background: var(--card); border: 1.5px solid var(--border);
  border-radius: var(--radius-xl); padding: 26px;
  box-shadow: var(--shadow);
}
.d-card-label {
  font-size: 0.72rem; font-weight: 700; color: var(--text3);
  letter-spacing: 1.2px; text-transform: uppercase; margin-bottom: 16px;
  display: flex; align-items: center; gap: 10px;
}
.d-card-label::after { content: ''; flex: 1; height: 1px; background: var(--border); }

.detail-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 16px; margin-bottom: 18px; }
.detail-pkg-name { font-size: 1.1rem; font-weight: 900; letter-spacing: -0.3px; color: var(--text); word-break: break-all; }
.detail-ver { font-family: var(--font-mono); font-size: 0.72rem; color: var(--text3); margin-top: 4px; }
.cve-badge {
  font-family: var(--font-mono); font-size: 0.72rem; font-weight: 600;
  padding: 6px 14px; border-radius: 100px;
  border: 1.5px solid rgba(255,107,107,0.25); background: rgba(255,107,107,0.08);
  color: var(--primary); white-space: nowrap; flex-shrink: 0;
}
.detail-desc {
  font-size: 0.88rem; color: var(--text2); line-height: 1.8;
  padding-top: 18px; border-top: 1px solid var(--border);
}

.fix-block {
  background: rgba(5,150,105,0.05); border: 1.5px solid rgba(5,150,105,0.2);
  border-radius: var(--radius); padding: 14px 16px;
  font-family: var(--font-mono); font-size: 0.78rem; color: var(--green);
  display: flex; align-items: center; justify-content: space-between; gap: 12px;
  margin-top: 18px;
}
.fix-label { font-size: 0.65rem; color: var(--text3); font-weight: 600; letter-spacing: 0.8px; text-transform: uppercase; margin-bottom: 6px; }
.fix-copy {
  background: transparent; border: 1.5px solid rgba(5,150,105,0.25); color: var(--green);
  padding: 5px 12px; border-radius: 100px; cursor: pointer;
  font-family: var(--font-sans); font-size: 0.72rem; font-weight: 600;
  transition: all 0.15s; white-space: nowrap;
}
.fix-copy:hover { background: rgba(5,150,105,0.08); }

/* Meta info */
.meta-row { display: flex; flex-wrap: wrap; gap: 10px; margin-top: 14px; }
.meta-chip {
  display: inline-flex; align-items: center; gap: 5px;
  font-size: 0.72rem; font-weight: 500; color: var(--text2);
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 100px; padding: 4px 12px;
}
.meta-chip strong { color: var(--text); }

/* Sidebar */
.detail-right { display: flex; flex-direction: column; gap: 14px; }
.owasp-sidebar-badge {
  border-radius: var(--radius); padding: 12px 14px; margin-bottom: 12px;
  background: rgba(220,38,38,0.05); border: 1.5px solid rgba(220,38,38,0.15);
}
.osb-code { font-family: var(--font-mono); font-size: 0.72rem; font-weight: 700; color: var(--red); margin-bottom: 4px; }
.osb-name { font-size: 0.88rem; font-weight: 700; color: var(--text); margin-bottom: 6px; }
.osb-desc { font-size: 0.8rem; color: var(--text2); line-height: 1.6; }

.sev-row { display: flex; flex-direction: column; gap: 8px; }
.sev-row-label { font-size: 0.72rem; color: var(--text3); text-transform: uppercase; letter-spacing: 1px; font-weight: 600; margin-bottom: 4px; }
.sev-bar { height: 6px; background: var(--surface); border: 1px solid var(--border); border-radius: 3px; overflow: hidden; margin-bottom: 10px; }
.sev-bar-fill { height: 100%; border-radius: 3px; }
.sev-error   .sev-bar-fill { background: var(--red);   width: 90%; }
.sev-warning .sev-bar-fill { background: var(--amber); width: 55%; }
.sev-info    .sev-bar-fill { background: var(--green); width: 25%; }

.action-stack { display: flex; flex-direction: column; gap: 8px; }
.act-btn {
  width: 100%; padding: 12px; font-family: var(--font-sans); font-weight: 700;
  font-size: 0.82rem; border-radius: 100px; cursor: pointer;
  transition: all 0.15s;
  display: flex; align-items: center; justify-content: center; gap: 8px;
  text-decoration: none;
}
.act-primary { background: var(--gradient); color: #fff; border: none; }
.act-primary:hover { opacity: 0.88; }
.act-secondary {
  background: transparent; color: var(--text2);
  border: 1.5px solid var(--border2);
}
.act-secondary:hover { border-color: var(--primary); color: var(--primary); }

@media (max-width: 1024px) { .detail-grid { grid-template-columns: 1fr; } }
@media (max-width: 768px)  { .detail-wrap { padding: 24px 20px; } }
</style>
{% endblock %}

{% block body %}
<div class="detail-wrap">

  <div class="breadcrumb">
    <a class="bc-link" href="{{ path('app_dashboard', {id: vuln.scanJob.id}) }}">Dashboard</a>
    <span class="bc-sep">›</span>
    <span>{{ pkg|length > 40 ? pkg|slice(0,40) ~ '…' : pkg }}</span>
    {% if cve %}
    <span class="bc-sep">›</span>
    <span style="color:var(--primary)">{{ cve }}</span>
    {% endif %}
  </div>

  <div class="detail-grid">
    <div class="detail-left">
      <div class="d-card">
        <div class="d-card-label">Vulnérabilité</div>
        <div class="detail-head">
          <div style="flex:1; min-width:0;">
            <div class="detail-pkg-name">{{ vuln.title }}</div>
            {% if version %}
            <div class="detail-ver">{{ version }}</div>
            {% endif %}
          </div>
          {% if cve %}
            <div class="cve-badge">{{ cve }}</div>
          {% endif %}
        </div>

        {% if vuln.description %}
        <div class="detail-desc">{{ vuln.description }}</div>
        {% endif %}

        <div class="meta-row">
          <span class="meta-chip">🔧 <strong>{{ vuln.tool }}</strong></span>
          {% if vuln.filePath %}
          <span class="meta-chip">📄 <strong>{{ vuln.filePath }}</strong></span>
          {% endif %}
          {% if vuln.lineNumber %}
          <span class="meta-chip">⌗ Ligne <strong>{{ vuln.lineNumber }}</strong></span>
          {% endif %}
        </div>

        {% if fixCmd %}
          <div style="margin-top:18px;">
            <div class="fix-label">Commande de correction</div>
            <div class="fix-block">
              <code>{{ fixCmd }}</code>
              <button class="fix-copy" onclick="navigator.clipboard.writeText('{{ fixCmd }}');this.textContent='✓ Copié'">Copier</button>
            </div>
          </div>
        {% endif %}
      </div>
    </div>

    <div class="detail-right">
      <div class="d-card">
        <div class="d-card-label">Catégorie OWASP</div>
        {% if vuln.owaspCategory %}
          <div class="owasp-sidebar-badge">
            <div class="osb-code">{{ vuln.owaspCategory|split(' — ')|first }}</div>
            <div class="osb-name">{{ vuln.owaspCategory|split(' — ')|last }}</div>
          </div>
        {% else %}
          <div style="color:var(--text3); font-size:0.85rem;">Non classifié OWASP Top 10</div>
        {% endif %}
      </div>

      <div class="d-card {{ 'sev-' ~ vuln.severity }}">
        <div class="d-card-label">Sévérité</div>
        <div class="sev-row">
          <div class="sev-row-label">Niveau de risque</div>
          <div class="sev-bar"><div class="sev-bar-fill"></div></div>
          {% if vuln.severity == 'error' %}
            <span class="sev-badge sev-c" style="width:fit-content">Error — Critique</span>
          {% elseif vuln.severity == 'warning' %}
            <span class="sev-badge sev-h" style="width:fit-content">Warning — Modéré</span>
          {% else %}
            <span class="sev-badge sev-l" style="width:fit-content">Info — Faible</span>
          {% endif %}
        </div>
      </div>

      <div class="d-card">
        <div class="d-card-label">Actions</div>
        <div class="action-stack">
          {% if refUrl %}
            <a href="{{ refUrl }}" target="_blank" class="act-btn act-primary">
              ↗ Voir l'advisory
            </a>
          {% endif %}
          <a href="{{ path('app_dashboard', {id: vuln.scanJob.id}) }}" class="act-btn act-secondary">
            ← Retour au dashboard
          </a>
        </div>
      </div>
    </div>
  </div>

</div>
{% endblock %}
