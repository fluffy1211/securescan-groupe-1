{% extends 'base.html.twig' %}
{% block title %}SecureScan — Créer un compte{% endblock %}

{% block stylesheets %}
<style>
.auth-wrap {
  width: 100%; min-height: calc(100vh - 60px);
  display: flex; align-items: center; justify-content: center;
  padding: 40px; position: relative; overflow: hidden;
}
.auth-blob { position: absolute; border-radius: 50%; filter: blur(80px); pointer-events: none; }
.auth-blob-1 { width: 400px; height: 400px; background: rgba(167,139,250,0.12); top: -80px; left: -80px; }
.auth-blob-2 { width: 350px; height: 350px; background: rgba(255,107,107,0.1); bottom: -60px; right: -60px; }

.auth-card {
  background: var(--card); border: 1.5px solid var(--border);
  border-radius: var(--radius-xl); padding: 44px 40px;
  width: 100%; max-width: 420px; position: relative; z-index: 1;
  box-shadow: var(--shadow-lg);
}
.auth-logo { text-align: center; margin-bottom: 32px; }
.auth-title { font-size: 1.6rem; font-weight: 900; letter-spacing: -0.5px; margin-bottom: 6px; color: var(--text); }
.auth-sub { font-size: 0.88rem; color: var(--text2); }

.auth-field { display: flex; flex-direction: column; gap: 6px; margin-bottom: 16px; }
.auth-label { font-size: 0.78rem; font-weight: 600; color: var(--text2); text-transform: uppercase; letter-spacing: 0.8px; }
.auth-input {
  background: var(--bg); border: 1.5px solid var(--border2);
  border-radius: var(--radius); padding: 11px 14px;
  font-family: var(--font-sans); font-size: 0.9rem; color: var(--text);
  outline: none; transition: border-color 0.15s, box-shadow 0.15s; width: 100%;
}
.auth-input:focus { border-color: var(--primary); box-shadow: 0 0 0 3px rgba(255,107,107,0.1); }
.auth-hint { font-size: 0.72rem; color: var(--text3); }

.auth-btn {
  width: 100%; padding: 13px; margin-top: 4px;
  background: var(--gradient); color: #fff;
  border: none; border-radius: 100px;
  font-family: var(--font-sans); font-weight: 700; font-size: 0.9rem;
  cursor: pointer; transition: opacity 0.15s;
}
.auth-btn:hover { opacity: 0.88; }

.auth-error {
  background: rgba(220,38,38,0.06); border: 1px solid rgba(220,38,38,0.2);
  border-radius: var(--radius); padding: 10px 14px; margin-bottom: 18px;
  font-size: 0.82rem; color: var(--red);
}
.auth-link {
  display: block; text-align: center; margin-top: 22px;
  font-size: 0.82rem; color: var(--text2);
}
.auth-link a { color: var(--primary); text-decoration: none; font-weight: 600; }
.auth-link a:hover { text-decoration: underline; }
</style>
{% endblock %}

{% block body %}
<div class="auth-wrap">
  <div class="auth-blob auth-blob-1"></div>
  <div class="auth-blob auth-blob-2"></div>

  <div class="auth-card">
    <div class="auth-logo">
      <div style="display:inline-flex;align-items:center;gap:8px;margin-bottom:14px;">
        <div class="nav-logo-mark">S</div>
        <span class="nav-logo-text">Secure<span>Scan</span></span>
      </div>
      <div class="auth-title">Créer un compte</div>
      <div class="auth-sub">Rejoignez SecureScan</div>
    </div>

    {% if error %}
      <div class="auth-error">{{ error }}</div>
    {% endif %}

    <form method="post">
      <div class="auth-field">
        <label class="auth-label" for="email">Email</label>
        <input class="auth-input" id="email" type="email" name="email"
               value="{{ app.request.request.get('email') }}" autocomplete="email" autofocus>
      </div>
      <div class="auth-field">
        <label class="auth-label" for="password">Mot de passe</label>
        <input class="auth-input" id="password" type="password" name="password" autocomplete="new-password">
        <span class="auth-hint">Minimum 8 caractères</span>
      </div>
      <div class="auth-field">
        <label class="auth-label" for="confirm">Confirmer le mot de passe</label>
        <input class="auth-input" id="confirm" type="password" name="confirm" autocomplete="new-password">
      </div>
      <button class="auth-btn" type="submit">Créer mon compte →</button>
    </form>

    <div class="auth-link">
      Déjà un compte ? <a href="{{ path('app_login') }}">Se connecter</a>
    </div>
  </div>
</div>
{% endblock %}
